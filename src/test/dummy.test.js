/* global describe, it */
const description = `This test can be removed, when test's from other branches will apear master` 
// but probably it will stay here for ever ^^
//
const expect = require('chai').expect
it('can be romeved', () => {
    const test = true
    expect(test).to.be.ok
})
